{
  "_comment": "Typecheck project for test/. The base config excludes test/, so a signature change cannot break its tests \u2014 see scripts/check-test-signatures.ts. Strictness the fixtures do not meet is relaxed here so the compiler still resolves every signature and reports arity. Widening what the gate reads means fixing that diagnostic's existing instances first.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "strictNullChecks": false,
    "noImplicitAny": false,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "exactOptionalPropertyTypes": false,
    "noUncheckedIndexedAccess": false
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "instrument/**/*.ts",
    "scripts/**/*.ts",
    "test/**/*.ts"
  ]
}
